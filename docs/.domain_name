sudo nano /etc/nginx/sites-available/drbrand.jervi.dev
```
server {
    server_name drbrand.jervi.dev;

    location / {
        proxy_pass http://localhost:6030;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_buffer_size 128k;
        proxy_buffers 4 256k;
        proxy_busy_buffers_size 256k;

        # Streaming optimizations
        proxy_http_version 1.1;           # Use HTTP/1.1 for better streaming support
        proxy_set_header Connection "";   # Clear Connection header to allow keep-alive
        proxy_buffering off;              # Disable buffering for real-time streaming
        proxy_cache off;                  # Disable caching to ensure fresh responses
        proxy_ignore_headers "Cache-Control" "Expires"; # Ignore backend caching headers
        tcp_nodelay on;                   # Send data immediately without delay
    }

    listen 443 ssl; # managed by Certbot
}
server {
    if ($host = drbrand.jervi.dev) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    server_name drbrand.jervi.dev;
    return 404; # managed by Certbot
}
```
sudo ln -s /etc/nginx/sites-available/drbrand.jervi.dev /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
sudo certbot --nginx -d drbrand.jervi.dev
sudo nano /etc/nginx/sites-available/drbrand.jervi.dev



